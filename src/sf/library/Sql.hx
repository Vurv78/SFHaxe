// Generated by SFHaxe 0.2.0
package sf.library;
@:native("sql") extern class Sql {
	/**
		CLIENT
		 Performs a query on the local SQLite database.
	**/
	#if CLIENT @:native("query") public static function query(query:Any):Any;#end
	/**
		CLIENT
		 Escapes dangerous characters and symbols from user input used in an SQLite SQL Query.
	**/
	#if CLIENT @:native("SQLStr") public static function SQLStr(str:Any, bNoQuotes:Any):Any;#end
	/**
		CLIENT
		 Checks if a table exists within the local SQLite database.
	**/
	#if CLIENT @:native("tableExists") public static function tableExists(tabname:Any):Any;#end
	/**
		CLIENT
		 Removes a table within the local SQLite database.
	**/
	#if CLIENT @:native("tableRemove") public static function tableRemove(tabname:Any):Any;#end
}

