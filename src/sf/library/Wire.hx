// Generated by SFHaxe 0.2.0
package sf.library;
@:native("wire") extern class Wire {
	/**
		SERVER
		 Returns the wirelink representing this entity.
	**/
	#if SERVER @:native("self") public static function self():Void;#end
	/**
		SERVER
		 Returns a wirelink to a wire entity
	**/
	#if SERVER @:native("getWirelink") public static function getWirelink(ent:Any):Any;#end
	/**
		SERVER
		 Creates/Modifies wire inputs. All wire ports must begin with an uppercase
		 letter and contain only alphabetical characters or numbers but may not begin with a number.
	**/
	#if SERVER @:native("adjustInputs") public static function adjustInputs(names:Any, types:Any):Void;#end
	/**
		SERVER
		 Wires two entities together
	**/
	#if SERVER @:native("create") public static function create(entI:Any, entO:Any, inputname:Any, outputname:Any, width:Any, color:Any, material:Any):Void;#end
	/**
		SERVER
		 Returns the server's UUID.
	**/
	#if SERVER @:native("serverUUID") public static function serverUUID():Any;#end
	/**
		SERVER
		 Unwires an entity's input
	**/
	#if SERVER @:native("delete") public static function delete(entI:Any, inputname:Any):Void;#end
	/**
		SERVER
		 Returns a table of entity's outputs
	**/
	#if SERVER @:native("getOutputs") public static function getOutputs(entO:Any):Any;#end
	/**
		SERVER
		 Creates/Modifies wire inputs/outputs. All wire ports must begin with an uppercase
		 letter and contain only alphabetical characters or numbers but may not begin with a number.
	**/
	#if SERVER @:native("adjustPorts") public static function adjustPorts(inputs:Any, outputs:Any):Void;#end
	/**
		SERVER
		 Returns a table of entity's inputs
	**/
	#if SERVER @:native("getInputs") public static function getInputs(entI:Any):Any;#end
	/**
		SERVER
		 Creates/Modifies wire outputs. All wire ports must begin with an uppercase
		 letter and contain only alphabetical characters or numbers but may not begin with a number.
	**/
	#if SERVER @:native("adjustOutputs") public static function adjustOutputs(names:Any, types:Any):Void;#end
}

