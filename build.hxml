

--library hre
--library json2object

# Link to SFHaxe, don't need to define lua-vanilla,
# SFHaxe does that for you.

--class-path src
--class-path src/generator
--main Main

--macro nullSafety("generator", Strict)

--each

--hl bin/hl
--next